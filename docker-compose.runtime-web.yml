services:
  signalum-web-preview:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    container_name: signalum-web-preview
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3300:3000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3000"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 20s
