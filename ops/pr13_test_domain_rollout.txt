cd /opt/messenger/contour-chat-jino-final && \

git apply patches/runtime-web-switch.patch && \

docker compose -f docker-compose.runtime-web.yml build --no-cache && \

docker compose -f docker-compose.runtime-web.yml up -d && \

docker compose -f docker-compose.runtime-web-safe.yml build --no-cache && \

docker compose -f docker-compose.runtime-web-safe.yml up -d && \

sudo cp infra/nginx/runtime-preview.test-domain.server.conf /etc/nginx/conf.d/<TEST_DOMAIN>.conf && \

sudo nginx -t && \

sudo systemctl reload nginx && \

curl -I http://127.0.0.1:3300/ && \

curl -I http://127.0.0.1:3301/
