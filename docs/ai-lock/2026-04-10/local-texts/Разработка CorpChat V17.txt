Продолжил. Собрал следующий этап уже как **launch-readiness pack** поверх ops-пакета: без раздувания scope, но с усилением того, что реально пригодится на живом запуске и показе. Это остаётся в рамках V17: один baseline, один deploy path, общий shell, голос/собрания, единый центр администратора и отсутствие лишнего хаоса.

Пакет:
[corpchat_v17_launch_readiness_pack.zip](sandbox:/mnt/data/corpchat_v17_launch_readiness_pack.zip)

Патч относительно `golive_ops`:
[corpchat_v17_launch_readiness.patch](sandbox:/mnt/data/corpchat_v17_launch_readiness.patch)

Что добил.

Во-первых, исправил реальную поломку в текущем контуре: в `infra/sql/006_operations_hardening.sql` был неверный `system_settings.value` вместо `system_settings.value_json`. Это как раз тот тип сбоя, который потом валит “финальный” релиз на живой БД. Сейчас это исправлено, и добавлен следующий SQL-шаг `007_launch_readiness.sql`.

Во-вторых, добавил то, что напрямую усиливает рабочий сценарий комнат и голоса:
- адресный вызов сотрудника в голосовую комнату или комнату собрания;
- персональный socket-room `user:{id}` для адресных событий;
- баннер быстрого перехода у вызываемого пользователя;
- комментарий к вызову;
- счётчики комнат в левом списке: участников и активного голоса.

То есть теперь “перетягивание” дополняется ещё и мягким вызовом в комнату, что полезно для живого командного сценария с большим числом людей.

Что изменил по backend:
- `voice.summon` в сервисе и роуте;
- адресная отправка события через socket;
- расширение `roomsRepository.getAllForUser()` и room detail статистикой;
- корректировка release defaults до `17.11.0 / launch-ready`.

Что изменил по frontend:
- счётчики рядом с комнатами в sidebar;
- обработка `voice:summon`;
- баннер “Быстрые вызовы в комнаты” с кнопкой перехода;
- кнопка `Вызвать в комнату` в VoiceRoomPage для модератора/админа.

Что проверил:
- `backend`: syntax check — пройден;
- `frontend`: production build — пройден.

Честно фиксирую ограничение.
Живой прогон на вашем Jino и браузерную проверку входа в голос я здесь не выполнял. По вашим же материалам внешний контур и `api/health` уже были подтверждены рабочими, а