# Шаблон обращения в поддержку Jino

Добрый день.

Для VPS с IP `81.177.141.214` включено проксирование HTTP(S). Домен `ai.voice.oboron-it.ru` указывает именно на этот IP.

Текущее состояние:
- HTTP работает корректно
- `http://ai.voice.oboron-it.ru/api/health` отвечает `200 OK`
- внешний TCP `80` открыт
- внешний TCP `443` закрыт (`Connection refused`)
- на самом VPS nginx и приложение работают
- backend доступен по HTTP через внутренний порт `80`

Нужно:
- чтобы запросы по HTTPS к `ai.voice.oboron-it.ru` публиковались снаружи и проксировались на внутренний порт `80` данного VPS

Просьба проверить:
1. применилось ли проксирование HTTP(S) именно к IP `81.177.141.214`
2. опубликован ли внешний порт `443` для этого IP
3. нет ли сбоя/неприменённого правила на стороне Jino для HTTPS publication
4. корректно ли привязан SSL-контур/сертификат к домену `ai.voice.oboron-it.ru`

Во вложении/приложении можно передать bundle диагностики с VPS.
